# Cognito user pool federated with Sign in with Apple (Phase 1).
# User pool + SignInWithApple identity provider + app client
# (authorization code grant + PKCE, no client secret) + hosted UI domain.

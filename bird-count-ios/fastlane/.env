# Loaded automatically by fastlane (dotenv). Applies to all lanes.
# xcpretty is no longer on PATH with newer fastlane and is deprecated in
# favor of xcbeautify (brew install xcbeautify).
FASTLANE_XCODEBUILD_FORMATTER=xcbeautify

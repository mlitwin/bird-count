import SwiftUI

/// Shared constants for view layouts
enum ViewConstants {
    /// Top padding to clear the floating AppHeaderView
    /// @deprecated Use HeaderSpacingView instead for dynamic header height calculation
    static let floatingHeaderTopPadding: CGFloat = 100
}
